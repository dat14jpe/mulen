#include "FeatureGenerator.hpp"

namespace Mulen {
    
    void FeatureGenerator::Generate(UpdateIteration& it)
    {
        // - to do: access to octree, and interaction with split/merge system
        // - to do: possibly split/merge features, and add them to octree nodes
    }
    
}
